<p style="color: green"><%= notice %></p>

<h1>Shifts</h1>

<div id="shifts" class="grid grid-cols-2 gap-4 sm:grid-cols-3">
  <% @shifts.each do |shift| %>
    <%= render shift %>
  <% end %>
</div>

<br>
